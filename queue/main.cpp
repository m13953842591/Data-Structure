#include "seqQueue.h"
#include "linkQueue.h"
#include <iomanip>
/*seqQueue<int> q;
for (int i = 0; i < 30; ++ i)
    q.enQueue(i);
for (int i = 0; i < 30; ++ i)
    printf("%d ", q.deQueue());
printf("\n");

linkQueue<int> q2;
 for (int i = 0; i < 30; ++ i)
    q2.enQueue(i);
for (int i = 0; i < 30; ++ i)
    printf("%d ", q2.deQueue());
printf("\n");*/
int main()
{
	cout << fixed << setprecision(4) ;
	cout << 0.0 << endl;
	return 0;
}
